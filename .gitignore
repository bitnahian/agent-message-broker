node_modules/
dist/
.nx/
.ralph/

# broker data files (must never be committed)
broker.db
broker.db-shm
broker.db-wal
*.db
*.db-shm
*.db-wal
*.sqlite
*.sqlite-shm
*.sqlite-wal

# test artifacts
test-results/
playwright-report/
*.tsbuildinfo

# vite cache
.vite/

# generated npm-package artifacts (rebuilt by packages/agent-message-broker/build.mjs)
packages/agent-message-broker/dist/
packages/agent-message-broker/ui/

# local agent skill installs
.dsh/

# Other harness project files (installed by global skill packages)
.claude/
.codex/


# agent iteration logs (regenerated per session, not project artifacts)
docs/implementation-log/

# OS files
.DS_Store
Thumbs.db

# Editor temp files
*.swp
*.swo
~*
storybook-static/

# coverage output
coverage/

# e2e coverage report (regenerated by playwright run)
packages/ui/e2e-coverage/

.secrets/*
# local env files (secrets for e2e / local runs)
.env
.env.local
.env.*.local

# local issue-tracker scratch (per-issue markdown, not project artifacts)
.scratch/

# internal, skill-derived docs — kept local, not for the public repo
/CONTEXT.md
/DESIGN.md
/PRODUCT.md
/docs/adr/
/AGENTS.md
/.impeccable/
/docs/
